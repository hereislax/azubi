[pytest]
DJANGO_SETTINGS_MODULE = Azubi.settings
python_files = tests.py test_*.py *_tests.py
addopts =
    --strict-markers
    --reuse-db
    --tb=short
markers =
    slow: langsame Tests (Migrationen, externe Services)
    integration: Tests die echte DB/Redis brauchen